## Gallery Display

* Gallery listing — grid view of image collections using FAL
* Year archive — filter galleries by year
* Category filtering — filter by `sys_category`
* Lightbox support — template hooks for lightbox integration

## File Management

* FAL integration — all images managed via TYPO3 File Abstraction Layer
